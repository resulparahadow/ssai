# macOS
.DS_Store
**/.DS_Store
# logs / scratch
*.log

# Claude Code local (per-user) settings — not project code
.claude/settings.local.json

# Supabase CLI local scratch (linked project, cached binaries) — not project code
supabase/.temp/
