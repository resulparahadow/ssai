{
  "name": "ssai-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Headless host for the legacy SSAI generation engine (runs the real legacy JS under Node, exact behavior). Laravel calls this over HTTP.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "smoke": "node smoke.js"
  },
  "engines": {
    "node": ">=20"
  }
}
